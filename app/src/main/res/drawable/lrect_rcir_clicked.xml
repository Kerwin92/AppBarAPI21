<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">

    <!-- 填充颜色 -->
    <solid android:color="@color/radiobutton_line"></solid>

    <!-- 显示一条实线，线的宽度为 width，颜色为 color -->
    <stroke
        android:width="@dimen/stroke"
        android:color="@color/radiobutton_line"/>

    <!-- 矩形的圆角半径 -->
    <corners
        android:bottomRightRadius="@dimen/cornor"
        android:topRightRadius="@dimen/cornor"/>

</shape>