<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

        <!-- Actionbar color -->
        <item name="colorPrimary">@color/transparent</item>
        <!--Status bar color-->
        <item name="colorPrimaryDark">@color/accent_material_light</item>
        <!--Window color-->
        <item name="android:windowBackground">@color/dim_foreground_material_dark</item>

        <!--将新加入的风格 AppTheme.MyDrawerStyle 设定给 drawerArrowStyle 属性-->
        <item name="drawerArrowStyle">@style/AppTheme.MyDrawerArrowStyle</item>

    </style>

    <!--加入一个新的 navigation drarwer 的风格-->
    <style name="AppTheme.MyDrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <!--將 spinBars 属性设定为 false-->
        <item name="spinBars">false</item>
        <!--设定 drawer arrow 颜色-->
        <item name="color">@android:color/black</item>
    </style>

    <style name="MyListView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:layout_below">@id/flyt_title</item>
        <item name="android:layout_above">@id/llyt_bottom</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:divider">#fafafa</item>
    </style>
    <!-- checkbox style for list -->
    <style name="mycheckbox" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@null</item>
        <item name="android:drawableLeft">@drawable/ckbox_selecter</item>
        <item name="android:paddingLeft">0dp</item>
        <item name="android:textSize">@dimen/third_font_size</item>
    </style>
    <!-- bottom button -->
    <style name="bottom_button_style" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/second_font_size</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_margin">15dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:background">@drawable/btn_style</item>
    </style>
    <!-- GifMovieView use -->
    <style name="Widget.GifMoviewView" parent="@android:style/Widget"></style>
</resources>
